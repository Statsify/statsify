{
  "name": "@statsify/assets",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "swc src  --config-file ../../.swcrc --out-dir dist --strip-leading-paths",
    "typecheck": "tsgo --noEmit",
    "lint": "oxlint",
    "lint:ci": "oxlint --format=github"
  },
  "dependencies": {
    "@statsify/logger": "workspace:^",
    "@statsify/schemas": "workspace:^",
    "@swc/helpers": "^0.5.23",
    "axios": "1.17.0",
    "skia-canvas": "3.0.8"
  }
}
